services:

  api-gateway:
    build:
      context: .
      dockerfile: ./api-gateway/Dockerfile
    ports:
      - "8000:8000"

  user-api:
    build:
      context: .
      dockerfile: ./sync/user-api/Dockerfile
    ports:
      - "8001:8000"

  book-api:
    build:
      context: .
      dockerfile: ./sync/book-api/Dockerfile
    ports:
      - "8002:8000"

  loan-api:
    build:
      context: .
      dockerfile: ./sync/loan-api/Dockerfile
    ports:
      - "8003:8000"

  fine-api:
    build:
      context: .
      dockerfile: ./sync/fine-api/Dockerfile
    ports:
      - "8004:8000"

  user-worker:
    build:
      context: .
      dockerfile: ./async/user-worker/Dockerfile
    # command: php artisan queue:work

  book-worker:
    build:
      context: .
      dockerfile: ./async/book-worker/Dockerfile
    # command: php artisan queue:work

  loan-worker:
    build:
      context: .
      dockerfile: ./async/loan-worker/Dockerfile
    # command: php artisan queue:work

  fine-worker:
    build:
      context: .
      dockerfile: ./async/fine-worker/Dockerfile
    # command: php artisan queue:work

  mysql:
    image: mysql:8.0
    container_name: mysql_db
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: root
    ports:
      - "3307:3306"

  rabbitmq:
    image: rabbitmq:3-management
    container_name: rabbitmq
    ports:
      - "5672:5672"
      - "15672:15672"

volumes:
  mysql_data:
